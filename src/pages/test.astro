---
import Layout from '../layouts/Layout.astro';
import { cn } from '../lib/utils';

import Card3 from '../components/cards/Card3.astro';

const cardCount = 1;
---

<script>
	import { parseNumberWithDefault } from "../lib/utils";


	declare global {
		interface Window {
			setCardIndex: (index: number) => void;
			getCardIndex: () => number;

			canGoToCard: (index: number,) => boolean;
		}
	}

	let globalCardIndex = 0;
	window.setCardIndex = (index: number) => {
		globalCardIndex = index;
	};
	window.getCardIndex = () => {
		return globalCardIndex;
	};

	const cardCount = parseNumberWithDefault(document.getElementById('main-container')?.dataset.cardCount, 1);

	window.canGoToCard = (index: number) => {
		return true;
	};
</script>


<Layout>
    <main
		id="main-container"
		class={cn(
			"text-white font-barlow"
		)}
		style={`min-height: ${100 * (cardCount)}vh;`}
		data-card-count={cardCount}
	>
		<Card3 class="z-100" cardIndex={0} cardCount={cardCount} />
    </main>
</Layout>
